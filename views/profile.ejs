<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Profile - <%= appName %></title>

        <!-- ICONSCOUT CDN -->
        <link rel="stylesheet" href="https://unicons.iconscout.com/release/v2.1.6/css/unicons.css" />

        <!-- GOOGLE FONTS (MONTSERRAT) -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />

        <!-- CSS -->
        <link rel="stylesheet" href="css/style.css" />
        <link rel="stylesheet" href="css/profile.css" />
        <style>
            body {
                background-image: url("images/bg-texture.png");
                margin: 0;
                padding: 0;
                font-family: "Montserrat", sans-serif;
            }
        </style>
    </head>


    <body>
        <%- include("./partials/navbar.ejs") %>
    
        <main class="profile-main">
            <div class="profile-container">
                <!-- Profile Header -->
                <div class="profile-header">
                    <img src="<%= user.profilePicture %>" alt="Profile Picture" class="profile-image" />
                    <h1 class="profile-name">Welcome, <%= user.displayName %></h1>
                </div>
    
                <!-- Profile Details -->
                <div class="profile-details">
                    <h2 class="profile-details-title">Your Details</h2>
                    <p class="profile-detail"><strong>Email:</strong> <%= user.email %></p>
                    <p class="profile-detail"><strong>Username:</strong> <%= user.username %></p>
                    <p class="profile-detail"><strong>Bio:</strong> <%= user.bio %></p>
                    
                    <button class="btn btn-primary" onclick="window.location.href='/edit-profile'">Edit Profile</button>
                    <!-- Logout Button -->
                    <form action="/logout" method="POST" style="margin-top: 1rem;">
                        <button type="submit" class="btn btn-secondary">Logout</button>
                    </form>
                </div>
            </div>
        </main>
    
        <%- include("./partials/footer.ejs") %>
    </body>
    </html>